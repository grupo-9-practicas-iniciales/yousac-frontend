import { SiUploaded } from 'react-icons/si'
import { IconBaseProps } from './IconProps.types';

export const LoaderIcon = (props: IconBaseProps) => {
    return (
        <SiUploaded {...props} />
    )
}
