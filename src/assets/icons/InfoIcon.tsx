import { IconBaseProps } from './IconProps.types';
import { AiOutlineInfoCircle } from 'react-icons/ai'

export const InfoIcon = (props: IconBaseProps) => {
    return (
        <AiOutlineInfoCircle {...props} />
    )
}
